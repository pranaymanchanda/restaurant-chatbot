/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local
.vercel
.wrangler

# Local secrets for `wrangler dev` — never commit real API keys.
# (.dev.vars.example is intentionally NOT excluded — it's a safe template.)
.dev.vars
.dev.vars.local
chatbot.jpeg
